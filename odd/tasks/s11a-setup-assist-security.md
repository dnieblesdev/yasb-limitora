# S11a Setup Assist transport security

## Goal
Resolve native finding `R1-nonce-predictable-transport` in the S11a candidate before publishing PR #298.

## Scope and constraints
- Work only in `F:/Vault/20_Desarrollo/20.10_Activos/yasb-limitora-s11a-setup-assist`, branch `feat/s11a-setup-assist`; preserve the primary and S11b worktrees.
- The user authorized a security-first Inno Setup API feasibility spike and now authorizes a disposable-VM proof only after C1 is fixed, current-candidate media are staged, and a fresh read-only preflight passes. This supersedes the earlier no-retest decision. Never execute an installer on the host.
- C1 is a separate follow-up to the transport correction; its earlier 200-line correction bound does not apply. The user chose explicit three-state controls (`unchanged`, `enabled`, `disabled`) for both `codex` and `opencode_go`, a required absolute Codex runner input when enabling Codex, and to retain the separate unchecked `configassist` consent gate.
- The current candidate (357 diff lines) plus the UI cannot fit the existing ≤400-line review slice. The user authorized two local work-unit commits: Slice 1 (transport + typed schema alignment, no provider-state mutation without a selection) and Slice 2 (explicit provider-selection UI), each separately reviewed and capped at 400 changed lines. This authorizes exactly those two local commits only; no push, PR, or other delivery. Ask before exceeding either slice.
- Keep `build/` ignored and preserve the primary and S11b worktrees.

## Acceptance
- Remove the attacker-controlled temp-file race/reparse path from the installer-to-assistant transport, or establish a demonstrably safe equivalent; malformed or unavailable input must fail closed.
- Preserve only the supported operations and consent boundaries; update the protocol contract and behavior-first tests with the implementation.
- Focused and full tests pass; Inno Setup 6.7.3 compile passes; real installer behavior and negative controls are verified only in the disposable VM.
- Native review closes the exact post-C1 candidate and its evidence is recorded. Commit/delivery remain separately gated by explicit user authorization.

## Tasks
1. [x] Map the transport, callers, tests, OpenSpec contract, and available APIs; CodeGraph and official Inno/Windows docs confirmed the environment+exit-code route is technically feasible.
2. [x] Finalize the bounded correction and contract/tests. The 194-line candidate closes the active nonce-only/file-transport dispatch path, updates the OpenSpec contract, and preserves the fail-closed schema mismatch for the planned C1 follow-up. The frozen-entry test now requires `_REQUEST_ENV`; a nonce-only CLI regression test lives in the protocol test file. Parent authorized the frozen-entry test adjustment after the full suite exposed its stale expectation.
3. [x] Independently run focused/full tests and compile-only ISCC 6.7.3; exact commands and compiler evidence are recorded below.
4. [pending — after C1] Re-audit every disposable-VM proof requirement, build/stage input media from the corrected S11a candidate, and run normal/adversarial scenarios only when all gates pass. Existing S11b media are stale and must not be used as proof for S11a. The user approved C1-before-VM to resolve the fail-closed dependency.
5. [x] Run fresh native review on the pre-C1 candidate and acknowledge approval. Lineage `review-af68f5fab5f6aeb5` approved and closed for target `sha256:363ea596dde9b3c4569fe8d94ca6e420b04e08f642ba95a72c0bbb730f26a7ef`; all four lenses completed, with advisory-only findings.
6. [x — Slice 1 implementation] Complete transport + typed-schema alignment without implicit provider changes: when the `configassist` consent gate is selected but there is no provider selection UI yet, do not emit a `config-apply` selection that changes provider state. Behavior-first tests and contract updates are in place; strict-TDD RED/GREEN and focused suite pass. Exact runner: `.venv/Scripts/python.exe -m pytest -q --strict-markers` from the S11a root, pytest 8.4.2.
7. [x] Slice 1 independent verification passed: full suite **957 passed, 3 skipped**; ISCC **6.7.3** compile-only passed; `git diff --check` clean; diff 350 lines across exactly seven paths. First authorized local work-unit commit: `1f37fa691050ad7e692cfa3878d1572ee9948f5e` (`fix(setup-assist): align typed request transport`). Native review lineage `review-b9564a80ca27d62a` approved and was acknowledged for target `sha256:56d3e788d2868238b7d9c20f9e012c5d992d2621a841f5b3110e91a104415d09`; no blocking findings or correction transition. No push/PR.
8. [x] Slice 2: add Inno UI for explicit `unchanged`/`enabled`/`disabled` states for `codex` and `opencode_go`, conditional absolute runner entry for Codex enable, retain unchecked `configassist` as separate opt-in, and update tests/spec/design. Candidate surfaces: `packaging/inno/yasb-limitora.iss`, `packaging/inno/SetupAssistant.isi`, `tests/test_inno_script.py`, `tests/test_setup_assist_protocol.py`, `openspec/changes/release-and-smoke-test-0-2-0/design.md`, `openspec/changes/release-and-smoke-test-0-2-0/specs/configuration-assistance/spec.md`, `openspec/changes/release-and-smoke-test-0-2-0/tasks.md`. Strict-TDD implementation complete; final candidate is 370 lines across seven files. Focused RED 1 failed/107 passed then GREEN 108 passed; independent focused 108, full 966 passed/3 skipped, `git diff --check`, and ISCC 6.7.3 compile-only all pass. Route: delegated direct; 4+ files to understand triggered `gentle-ai-explore`, followed by one bounded `gentle-ai-worker`.
9. [x] Slice 2 native review closed. Final independent verification passed on 370 lines/seven paths (focused 108, full 966 passed/3 skipped, diff check clean, ISCC 6.7.3 compile exit 0). Second authorized local commit created: `93aa64992e2eda77de9e5ef9f1cd00366ab4637d`. Exact-range native lineage `review-d4a7051143c3334d`, target `sha256:90c5c7e10fd4cc6cf59c2262226e7da623a0ff3d7ac1ae0a35a62cf6f5ebc82d`, all four lenses (`review-risk`, `review-resilience`, `review-readability`, `review-reliability`) captured through the provider `pi_host_relay` group route. Result: **approved**, no correction transition, ten advisory/informational findings. Authority burned with `gentle-ai.review-acknowledged/v1` at consumed revision `sha256:3927caee48a47c606959eab4c943f34465152cd6aa0b40b83df14e73e05fe6d7`. No push/PR/VM.
   - Earlier capture rejections in this lineage were a host session-route artifact, not a dead transaction or a provider block: retained capture routes live only in the Pi extension process for the live session id, and are dropped when that session is replaced or Pi exits. The recovery that worked was one fresh bound `review status` in the working session (which re-registers the current collect bindings as routes) followed by the exact group bindings. A fresh STATUS also proved the earlier `native-operation-failed`/`error_code=cancelled` envelope was an aborted (cancelled) native process, never a verdict about the frozen candidate. Inventing a correction was neither needed nor used: the correction budget (185) was never consumed.
10. [x] Disposable-VM proof completed on the corrected candidate. Storage preflight and a bounded 2 GiB write probe on D: passed (107.6 MB/s, real allocation, 0 mismatched blocks, 0 new disk/51 events). Fresh S11a media was built from `b363abe` (`s11a-inputs-r2.iso`, sha256 `bee1b386...`, installer `ddfa6761...`/13491710, three host gates PASS) and a new checkpoint `clean-windows-running-watcher-s11a-v2` was frozen with that media attached. Six scenarios ran (`first-install`, `reinstall`, `locked-file`, `uninstall-preserve`, `explicit-cleanup`, `assist-request`): 10 run records, every `setup` record executed `ddfa6761...`, every `setup`/`uninstall` step exited 0, every guest shut down cleanly, and every `watcher.log` reports the frozen stamp `57d7ce68...`. The host gate `build/s11a-lifecycle/verify-evidence.py` returns PASS and an independent read-only verification confirmed coverage, media identity, exit codes, consent answers (`User chose No.`/`Yes.`) and locked-file tolerance.
11. [x] VM outcome recorded with caveats. **Open findings, not blockers for the recorded verdict:** (a) `explicit-cleanup` did not remove state — the uninstall `state-cleanup` assist logged `[yasb-limitora setup-assist] assistant reported non-zero exit` and the fixture tree remained; the scenario proves the dialog answer reached the prompt, not that cleanup ran. (b) `config-apply` is unreachable in a silent run: with no provider choice, the installer emits only `{"operation":"discover"}`, so the assist proof covers the request/env/exit-code transport, not the provider mutation. (c) the assist child's stdout/stderr are hidden, so a non-zero assist exit records no reason. (d) the guest watcher cannot fail a cycle closed (`Get-ObjectProperty` reads `PSObject.Properties`, which never exposes OrderedDictionary keys), so `done.json result: success` is not a failure gate; the host gate is. No push, PR or merge was performed.
12. [x] Assist reason channel committed, then corrected after native review. Commit `5490a506e4236fd689f0e9b49c83bb8267dc6344` (4 files, 248/3) added a bounded reason channel for a non-zero assist exit plus host scenario `10-owned-cleanup.json`; verification at commit time: focused 115 passed, full suite 978 passed/3 skipped, `git diff --check` clean, `validate-guest-paths.ps1` and `validate-scenarios.ps1` pass. `roundtrip-verify.ps1` still expects `s11a-inputs.iso` and must be repointed at the r3 media.
13. [x] Fresh native review of the exact reason-channel range (`baseRef=b363abe`, committed-only) opened lineage `review-d5752d4df01d386c`, target `sha256:20afdef12a30e3be8bd9504c7bd02fa618cba07af5fbf5c38022e250234c3150` (4 files, 251 changed lines, high tier, frozen correction budget 126). All four lenses were captured through the provider `pi_host_relay` group route; the result was **`correction_required`** with two candidate-caused deterministic CRITICALs: `R1-001` (`packaging/inno/SetupAssistant.isi:205`, unbounded `LoadStringFromFile` of a predictable temp-dir reason file read by the elevated installer) and `R1-002` (`src/yasb_limitora/setup_assist.py:607`, elevated child writes to an environment-supplied absolute path with no reparse/exclusivity check). Both are the same root cause the whole task exists to remove: a file channel in a user-writable directory inside the elevated path.
14. [x] Bounded correction for R1-001/R1-002, design chosen by the user: **remove the installer-side file channel and log the numeric exit code**. The full removal (both sides plus their tests) measures ~224 diff lines and cannot fit the frozen 126-line budget, and native permits only one correction per lineage, so the correction lands as: installer side removed (`YasbSetupAssistReasonEnvironment`, `YasbSetupAssistMaxReasonBytes`, `YasbSetupAssistReadBoundedReason`, the reason path/read/log/finally in `YasbSetupAssistRun`) plus `assistant reported non-zero exit (code N)`; `src/yasb_limitora/setup_assist.py` is left untouched and its `_write_reason` becomes unreachable — production sets `_YASB_SETUP_ASSIST_REASON` only from the installer, and a user who sets it by hand writes with their own privileges, crossing no privilege boundary. Correction surfaces: `packaging/inno/SetupAssistant.isi`, `tests/test_inno_script.py`.
   - Landed as commit `c04be6ebf4caf557a50f4fdc9771d2f9e65964ae` on `feat/s11a-setup-assist` (89 diff lines: 28 insertions, 61 deletions; inside the declared 96-line plan) after the user authorized that local commit. The installer now logs `assistant reported non-zero exit (code N)`.
   - Independent verification passed: focused `tests/test_inno_script.py` 44 passed; full suite 978 passed / 3 skipped; `git diff --check` clean; only the two allowed paths changed; ISCC 6.7.3 compile exit 0 with the required `/DAppVersion`, `/DSourceDir`, `/DOutputDir` defines. The verifier's first authorized ISCC command lacked those defines, aborted at `PreprocessingError` on line 3 and produced no parse evidence; the verifier refused to invent defines instead of working around the command, which is the correct behavior. The corrected compile produced a fresh artifact and the preprocessor abort disappeared.
   - Evidence limits recorded honestly: the quiet ISCC invocation suppresses stdout, so neither the `Line 54: Including file: ...SetupAssistant.isi` line nor compiler warnings could be captured verbatim for this specific run; success rests on exit 0 plus a freshly written setup artifact. Also, the assistant-side reason tests number **seven**, not six as earlier wording said; all seven pass because that file is untouched.
17. [open — needs a human decision] The native lineage `review-d5752d4df01d386c` **escalated** after the correction instead of closing. STATUS now reports `state: escalated`, `action: stop`, `next_transition: stop / native_stop_required`, `escalation.cause: targeted_validator_rejected`, `finding_ids: [R1-001, R1-002]`. The corrected candidate (`correction_target_identity sha256:dad59ac0…`, tree `cc6a044c…`) was accepted as the correction candidate and the slot was reoffered; the targeted-validator relay then failed twice at **admission**, never consuming the slot (`mutation_outcome: none`): first `Error: decode provider targeted validator result: json: unknown field "evidence_note" [invalid_request]`, then a plain `native-operation-failed` with `lineage_created: false` and no verdict. This is a provider-relay/schema failure on the validator path, not a verdict about the corrected candidate, and it leaves the candidate reviewed-by-nobody: the earlier `b363abe` authority is burned and this lineage never reached approval. Per the stop table, `native_stop_required` is terminal for maintainer inspection or the human-owned switch disable, and a fresh lineage is a human decision — not an automatic retry.
15. [x — no longer a follow-up] The dead writer was deleted inside the review route rather than as a separate work unit: commit `8681dd2d086c1a0a96802d0228b420eeed359eee` removes `_REASON_ENV`, `_MAX_REASON_BYTES`, `_write_reason`, its 11 call sites and both now-pointless reason loops, plus the six assistant-side tests that only worked by injecting the environment (131 diff lines: 3 insertions, 128 deletions). Focused 65 passed, full suite 972 passed / 3 skipped, `git diff --check` clean.
16. [pending] Rebuild media r3 from the reviewed candidate (`8681dd2`), repoint `roundtrip-verify.ps1` at `s11a-inputs-r3.iso`, re-freeze a checkpoint carrying that media (one operator login), then rerun the 7 scenarios including `owned-cleanup` and verify the host gate plus an independent read-only pass.
18. [x] Fresh lineage `review-81509f849ed59200` over the candidate state `b363abe..c04be6e` (216 lines, frozen budget 108) admitted all four lenses and returned **`correction_required`** with four findings on the residual dead writer — `R1-001` risk BLOCKER and `R3-001` reliability BLOCKER (an environment-supplied absolute path an attacker could redirect under the assistant's privileges), `R2-001` readability CRITICAL (the channel is dead in the installer path and its tests passed only by injecting the environment) and `R3-002` reliability CRITICAL (a write before nonce validation, contradicting its own comment). The honest correction is the 131-line deletion in item 15, and the provider **refused it mechanically at admission** — `actual correction is 131 changed lines, exceeding the frozen budget of 108` — without consuming the slot, because the frozen budget is half the candidate and the six obsolete tests alone are 85 diff lines. That is a structural mismatch, not a fixable measurement: no within-108 variant fixes those four findings and keeps the suite green.
19. [x] The same final state, reviewed as a **candidate** instead of as a correction, closed cleanly. Fresh lineage `review-33481bc761a5dcdc`, exact range `b363abe..8681dd2` (91 changed lines, 89 insertions / 2 deletions, four paths, tier high, budget 46): the group capture failed with a model-provider `429` (`prepared_reviewers: 0`, no mutation), so all four lenses were captured **one slot at a time**, each admitted `completed`. Result: **approved**, no correction transition, with three informational `SUGGESTION` findings only (`R2-001` `tests/test_setup_assist_protocol.py:591-594`, `R2-002` `tests/test_inno_script.py:647-649`, `R2-003` `tests/test_inno_script.py:666-677`). The exact provider-issued acknowledgement was executed: `authority: burned`, `burn_evidence: gentle-ai.review-acknowledged/v1`, consumed revision `sha256:7f61bb7f32e2df3a04c543ded4263ad637f9235a5f0bb8789e5c9107cd8216e3`, `delivery: ordinary-repository-policy`. No STATUS was called after the burn. Two lineages remain unresolved on purpose: `review-d5752d4df01d386c` (`escalated`, validator admission failure) and `review-81509f849ed59200` (`correction_required`, un-fundable correction).
   - Net effect on the branch: the reason-channel experiment nets out. `b363abe..8681dd2` adds only the numeric exit code to the installer log, the installer-side regression tests, and one assistant-side guard asserting that an environment naming an absolute reason path writes nothing. The history still carries `5490a50` → `c04be6e` → `8681dd2` if anyone reads it; collapsing that churn is a delivery-plan option, not a review one.
20. [x] r3 media rebuilt, frozen into a new checkpoint, and proved with the full seven-scenario cycle. Payload re-frozen from `8681dd2` with the S11a `.venv` (`scripts/build_frozen_bundle.py`, no arguments, `build-info.source_commit` = `8681dd2`), installer rebuilt with ISCC (`683c123e1fb909a3971df99d4e7e8df179cf77c3dbf435ff865bb32066e38d58`, 13,492,025 B), ISO regenerated after correcting the stale identity inside the ISO member `iso-staging/README.md` (`s11a-inputs-r3.iso`, `b4d323d17da42bb92994ca1da3e9b17226de0843682d2c3fc78475715f92a6da`, 13,641,728 B), `custody.json` and `manifest.json` rewritten, and the three host gates re-run PASS (guest-paths, scenarios 7/0, roundtrip 5/0) twice independently. Checkpoint `clean-windows-running-watcher-s11a-v3` (`89a47039-2230-4f0f-9c20-640e0163f40a`) was frozen with the r3 ISO attached at SCSI 0/2; the restore left the VM `Saved`, so the media swap happened with the guest not executing at all. `run-orchestrator.ps1` was repointed at v3 and the r2-era evidence archived to `evidence-r2-superseded/` + `evidence-vhdx-r2-superseded/`.
21. [x] Seven-scenario r3 cycle completed: `ORCHESTRATOR FINISHED SUCCESSFULLY (7 cycle(s))`, every cycle `success` (first-install, reinstall, locked-file, uninstall-preserve, explicit-cleanup, assist-request, owned-cleanup), 12 run records, and the host gate `verify-evidence.py … --expected-executable 683c123e…` → `VERDICT: PASS` (exit 0). Independent read-only verification confirmed: all seven `setup` records carry the r3 installer `683c123e…`/13,492,025 and none carries `ddfa6761…`, `7ff33e5d…` or `96011417…`; coverage matches the seven declared manifests 1:1; every scenario records the frozen watcher stamp `57d7ce68…` and a clean guest shutdown (121–145 s, well under the 600 s bound); all run-record exit codes are 0; `locked-file` shows the genuine lock, one bounded retry and exit 0; `owned-cleanup` shows consent `User chose Yes.`, no assist failure line, and the after-capture with the state tree gone (`present=False`, `fileCount=0`); `explicit-cleanup` now shows the new line `assistant reported non-zero exit (code 1)` with the fixture state retained; `assist-request` remains thin (the emitted operation payload is nowhere in the evidence, matching the documented `config-apply` limit).
    - Corrections and honest limits recorded from that verification: the r3 ISO **stays attached to the VM DVD drive** by design (the harness never attaches or detaches media and each cycle reverts the checkpoint), so "nothing left attached" holds for the host but not for the VM; the `owned-cleanup` attribution to the assist is an inference from the after-capture rather than a recorded assist line; the refusal cause in `explicit-cleanup` is no longer recorded anywhere now that the reason channel is gone; `verify-evidence.py` does not check watcher stamps, ISO identity, guest state content or assist behaviour, so its PASS covers exit codes and the setup hash only; and `roundtrip-verify.ps1` check (d) cannot fail (it calls `Get-DiskImage` without the mandatory `-ImagePath` and swallows the throw), which the harness README now documents along with the re-check that replaces it.
22. [x] S11a delivery opened as three stacked PRs and #298 retired. PR #298 was **closed as superseded** — its head `bd31f71` carried the pre-correction transport, i.e. the candidate native review refused with the CRITICAL `R1-nonce-predictable-transport` — so no reviewer or CI run could merge the vulnerable transport by accident. Branches were created at the existing commit boundaries (no rebase, no force-push, every SHA preserved) and pushed:
    - PR #300 `feat/s11a-transport-typed-request` → `main`, 707 lines (677/30), 11 files, `type:feature`
    - PR #301 `feat/s11a-provider-selection` → PR #300's branch, 370 lines (346/24), 7 files, `type:feature`
    - PR #302 `feat/s11a-installer-hardening` → PR #301's branch, 141 lines, 5 files, `type:bug`
    All three link the approved issue #297 and carry exactly one `type:*` label.
23. [x] CI caught a real defect on PR #302's first run, which is exactly what the required check is for: `test_scenario_10_owned_cleanup_shape` asserted that `build/s11a-lifecycle/scenarios/10-owned-cleanup.json` exists, but `build/` is git-ignored, so a clean clone carries no harness and the test failed there (`1 failed, 971 passed, 3 skipped`). Fixed in `5cc45b2` (`fix(tests): skip the harness-dependent scenario test when build/ is absent`): the test now skips exactly when the harness is absent, and the fix was proven in a clean `git clone --shared` with no `build/` — the test skips and the suite is 971 passed / 4 skipped. After the push all three PRs are green (`native-proof` pass at 1m26s / 1m45s / 1m50s).
24. [x] Native review of the CI fix closed: lineage `review-079881b1ce5b5384` (candidate `8681dd2..5cc45b2`, one file, 15 changed lines) **approved** with two informational WARNINGs at `tests/test_inno_script.py:682-683` (`R2-001` readability and `R4-001` resilience, both about the skip guard), acknowledgement executed, authority burned (`gentle-ai.review-acknowledged/v1`, consumed revision `cbbf5b3877b…`).
25. [open — recorded honestly] The three advisory `SUGGESTION`s from the approved review of the net candidate (`tests/test_setup_assist_protocol.py:591-594`, `tests/test_inno_script.py:647-649`, `tests/test_inno_script.py:666-677`) cannot be implemented faithfully yet: the facade reported only id, lens, location, severity and disposition, never the claim text, and that lineage's authority is burned. The honest route is the follow-up change's own review, which will raise its findings with their text, rather than guessing at the originals.
26. [pending — user-owned] Merge the three PRs in order (they must land bottom-up), and only then continue with the S11b/B1/C5 chain. No merge was performed and no release action was taken.
27. [x] Advisory and harness findings parked as issues instead of a correction PR, by user decision: **#303** `test(s11a): address the advisory review findings on the delivered test surface` (`enhancement`), **#304** `fix(proof): make the disposable-VM verification gates fail closed` (`bug`), **#305** `docs(s11a): record the r3 VM proof identities and what it does not prove` (`documentation`). All three are open and none carries `status:approved`, which is the user's gate to start the work and a requirement for any PR that links them. Rationale: the facade never exposed the advisory findings' claim text, so a correction PR could only have guessed at it.
28. [x] **Correction to the reconciliation premise, verified with git.** The earlier assumption that C1/C2/C3 merely duplicate the S11a slices is wrong. The merge base is `bd31f71`, and the S11b-only commits carry their own stats: `6b87885` (C1, 3 files, +303/−84), `0646725` (C2, 5 files, +76/−104), `a50335c` (C3, 4 files, +43/−17), `69c7ff5` (4 files, +402/−8) plus the docs/evidence commits. Verified content facts: `packaging/inno/` on the S11a head contains **no** `path-remove` at all, while the S11b head emits `YasbSetupAssistOperationsPathRemove` and the `path-remove` + `state-cleanup` uninstall sequence — so C1's uninstall fragment is genuinely missing; `cleanup_literal_state` takes `(registry, state_dir)` on the S11a head and `(state_dir)` on the S11b head, so C2 is absent; and C3 deletes `configassist`/`ConfigWizardConsent`, which Slice 2 deliberately added under an explicit user decision, so C3 must not be carried.
29. [open — needs a product decision] State-cleanup behaviour differs between the branches and the two recorded proofs disagree about it: S11a's r3 evidence documents the ownership-gate refusal as correct (`explicit-cleanup` keeps the fixture state and logs `(code 1)`), while the S11b lifecycle evidence records `explicit-cleanup (YES) PASS with the state root deleted`, which only holds under C2's registry-free cleanup. Carrying C2 forward reverts a deliberate S11a decision and invalidates that evidence; dropping C2 means rewriting the S11b evidence. That is a user decision, not a mechanical merge choice.
30. [x] S11b reconciliation executed and independently verified, still uncommitted, on branch `feat/s11b-transaction-closeout-v2` created from `5cc45b2` in the S11b worktree. Carried: the uninstall `path-remove` fragment of C1 adapted to the S11a environment transport (uninstall now always emits `[{"operation":"path-remove"}]` and adds `state-cleanup` only with consent, while `state-cleanup` keeps requiring the registry ownership proof), and `69c7ff5` by cherry-pick (`HasOwnedNewUninstaller` replacing the textual-inequality ownership test, the fail-closed reordered `DelTree`, `scripts/build_setup.py` plus its tests). Dropped by user decision: C2 and C3. Verified independently: net delta exactly four files (391 insertions / 9 deletions) plus two untracked ODD records; every one of the nine removed lines is explained by its intended replacement; no S11a feature was lost; `cleanup_literal_state(registry, state_dir)` and the `state-record-missing` refusal are intact; the provider page is intact; ISCC 6.7.3 compile-only exit 0; full suite **982 passed / 4 skipped**.
    - Runner pitfall, recorded again because it already produced false evidence once: the **system** python resolves `yasb_limitora` to the primary worktree and yields six phantom failures in `tests/test_setup_assist_protocol.py`; the S11a `.venv` resolves to the S11a worktree; only `PYTHONPATH=<worktree>/src` makes it import the worktree under test. Any test evidence for a worktree other than S11a must state its runner, and a plain `python -m pytest` there is not evidence.
    - Note for the record: the +10 passed / +1 skipped delta against the S11a baseline is expected (the build-driver tests plus the scenario-shape skip, since this worktree has no `build/s11a-lifecycle/` harness) but the baseline was not re-run, so that decomposition rests on inspection.
31. [pending] Re-author the S11b documentation and evidence before its PR: every tree-bound citation (commit SHAs, the `7ff33e5d…` setup hash, checkpoint names, line references, and the claim that "C1–C3 were delivered on the S11a side") is false on the new base, and the `explicit-cleanup` evidence must record the ownership-gate refusal as the correct outcome. Then verification, native review and the S11b PR on top of `feat/s11a-installer-hardening`.
32. [pending — needs user authorization] Commit the reconciliation as work-unit commits (the C1 fragment with its renamed test, then the closeout cherry-pick), and decide whether the two ODD task records join the branch.

## Slice 2 mapping and resolved UI choice
- Read-only map completed by delegated `gentle-ai-explore` task `mugcrsdm-1-oe1f`; all ten requested S11a files exist. Python `config-apply` validation/merge and CLI dispatch already support the needed semantics, so no Python source change is planned.
- Natural insertion points: add the provider page in `InitializeWizard` after task selection; capture choices in the installer consent flow; append a typed `config-apply` selection from `InvokePostCommitAssist` only when `configassist` is selected and at least one provider changes from `unchanged`. `unchanged` means omitting that provider key; empty selection is invalid, so all-unchanged should retain the existing `discover` fallback. Codex enable requires an absolute runner path. Use a browse input without adding another `Exec` call.
- Existing Slice-1 tests that ban `config-apply` must be narrowed to assert no provider operation without explicit selection and rewritten to cover the selected operation and typed `selection`. Add coverage for tri-state defaults, gated page, escaping/absolute Codex path, and unchanged-key omission. Update OpenSpec design/spec/tasks.
- User resolved the material UI question: show the provider-selection page only when `configassist` is checked; both provider controls default to unchanged. The configassist opt-in remains a separate apply gate.
- Route is delegated direct: 4+ file mapping via `gentle-ai-explore`, then one bounded multi-file `gentle-ai-worker`; strict TDD is enabled, runner `.venv/Scripts/python.exe -m pytest -q --strict-markers`. Initial candidate (298 lines) failed ISCC 6.7.3 due to unsupported `TOpenDialog`; a behavior-first fix replaced it with Inno's built-in file-input page. A later refinement supports drive-rooted and UNC Codex runner paths. Final candidate is 370 lines across seven planned paths; strict-TDD RED 1 failed/107 passed, GREEN 108 passed, full 966 passed/3 skipped, `git diff --check` clean, and independent ISCC 6.7.3 compile succeeded. Second commit is `93aa64992e2eda77de9e5ef9f1cd00366ab4637d`; its exact-range native review `review-d4a7051143c3334d` completed all four lenses, was approved, and was acknowledged (authority burned). Scout estimated 180–330 lines before the UNC refinement; the Slice-2 cap remains ≤400.

## Test environment — corrected S11a runner
- The active config enables strict TDD (`strict_tdd: true` in `C:\\Users\\dnieb\\.gentle-ai\\state.json`). `openspec/config.yaml` defines the canonical invocation as `python -m pytest -q --strict-markers`.
- A read-only diagnosis found that every global Python's editable install resolves `yasb_limitora` from the primary worktree, not S11a; running the global command from S11a would test the wrong source. The S11a `.venv` correctly resolves its own `src` but initially had pytest 9.1.1, outside `pyproject.toml`'s `pytest>=8,<9` constraint.
- Parent aligned only the ignored S11a `.venv` via `uv 0.12.18 pip install --python .venv/Scripts/python.exe 'pytest>=8,<9'`; pytest is now 8.4.2. No source, lockfile, or global environment changed. Slice 1 RED/GREEN, focused tests, and full suite were run after the environment correction. Use `.venv/Scripts/python.exe -m pytest -q --strict-markers` from the S11a root for subsequent checks.

## C1 candidate status — two-slice plan authorized
- An initial C1 writer candidate hardcoded `{"codex":{"enabled":true}}` behind the consent checkbox; that behavior was rejected. Its first global-Python test run was misleading because the editable install imported the primary worktree. A read-only tooling diagnosis confirmed the wrong import root.
- Slice 1 now emits typed operation objects without `choices` and omits `config-apply` until explicit provider-state UI exists; the separate `configassist` checkbox cannot mutate providers. The installer design contract and behavior tests were updated.
- Slice 1 worktree diff is 350 lines (263 additions/87 deletions) across seven paths: `design.md`, `SetupAssistant.isi`, `cli.py`, `setup_assist.py`, `test_frozen_entry_order.py`, `test_inno_script.py`, and `test_setup_assist_protocol.py`. Strict TDD observed RED (1 failed, 93 passed) then GREEN/focused (94 passed) using S11a `.venv` Python 3.14.7 + pytest 8.4.2 and `.venv/Scripts/python.exe -m pytest -q --strict-markers`.
- Independent verification passed: full suite **957 passed, 3 skipped** with `.venv/Scripts/python.exe -m pytest -q --strict-markers tests`; ISCC **6.7.3** compile-only succeeded into ignored `build/iscc-verify/`; independent `git diff --check` exited 0. No installer or VM execution. Diff inspection confirmed the active Inno typed operations pass Python's strict validator, `config-apply` is absent until explicit UI, and the 350-line candidate stays within the 400-line slice.
- First authorized local work-unit commit is `1f37fa691050ad7e692cfa3878d1572ee9948f5e`. Native review lineage `review-b9564a80ca27d62a` was scoped to its exact seven-file committed range (parent `72d18ca069e39fbce671830abcd124a85e5a1977`), target identity `sha256:56d3e788d2868238b7d9c20f9e012c5d992d2621a841f5b3110e91a104415d09`; four reviewers completed and the review was approved and acknowledged. Nine advisory findings were informational/non-blocking (readability: five; reliability: three; resilience: one). No correction was offered. No push/PR.
- The user resolved the UI semantics: both providers; explicit unchanged/enabled/disabled states; absolute runner input required for Codex enable; preserve the unchecked `configassist` checkbox as separate consent. The first candidate's hardcoded Codex enablement was removed from Slice 1; Slice 2 will emit provider selections only from the explicit UI choices. Parent measured the previous diff as 357 lines; Slice 1 is now 350 lines. Read-only forecast estimates 170–385 lines for the separate UI/tests/contracts slice. The user authorized two separate local work-unit commits, one per slice, without push/PR.

## Native correction and fresh review
- Earlier lineage `review-b5775d638257b9fc` identified CRITICAL `R1-nonce-predictable-transport`, accepted a 200-line correction plan, and later stopped at `corrected_candidate_unavailable`; its status and plan are stale for the current candidate.
- Fresh lineage `review-af68f5fab5f6aeb5` reviewed target `sha256:363ea596dde9b3c4569fe8d94ca6e420b04e08f642ba95a72c0bbb730f26a7ef` as high risk (194 changed lines; frozen correction budget 97). Risk, resilience, readability, and reliability lenses all completed; the result is approved with no blocking findings and no correction transition.
- Advisory-only findings: `R1-AUTHZ-ENV` WARNING (`src/yasb_limitora/setup_assist.py:596-609`); `R2-1` WARNING (`setup_assist.py:6-8`); `R2-2` SUGGESTION (`setup_assist.py:605-607`); `R2-3` SUGGESTION (`packaging/inno/SetupAssistant.isi:223`); `R3-001` WARNING (`tests/test_setup_assist_protocol.py:395`); `R3-002` WARNING (`setup_assist.py:609`); `R3-003` WARNING (`setup_assist.py:115`); `R4-001` WARNING (`SetupAssistant.isi:223-224`). The provider declared all findings informational and non-blocking.
- The exact approved continuation was acknowledged; native authority is burned and this pre-C1 review is closed. No further STATUS was called after acknowledgement. Slice 1 is now separately implemented; the explicit provider-selection UI is implemented and reviewed below; VM proof remains pending.
- Slice 2 lineage `review-d4a7051143c3334d` (target `sha256:90c5c7e10fd4cc6cf59c2262226e7da623a0ff3d7ac1ae0a35a62cf6f5ebc82d`, 370 changed lines, high tier, frozen correction budget 185) completed all four lenses and is **approved** with no correction transition. Ten advisory findings, all `informational`: `R1-json-escape-controls` SUGGESTION (`packaging/inno/SetupAssistant.isi:228`); `R2-runner-guard` SUGGESTION (`SetupAssistant.isi:265`); `R2-test-slice-window` SUGGESTION (`tests/test_inno_script.py:570`); `R2-tri-state-encoding` WARNING (`SetupAssistant.isi:257-273`); `R3-001` WARNING (`tests/test_inno_script.py:518-531`); `R3-002` WARNING (`tests/test_setup_assist_protocol.py:454-464`); `R3-003` SUGGESTION (`tests/test_inno_script.py:562-585`); `R3-004` SUGGESTION (`SetupAssistant.isi:228-243`); `R4-001` WARNING (`SetupAssistant.isi:264-278`); `R4-002` SUGGESTION (`packaging/inno/yasb-limitora.iss:298`). Provider statement: every finding is non-blocking, none opened a correction, and none reopens this review. The exact provider-issued acknowledgement was executed; `authority: burned`, `burn_evidence: gentle-ai.review-acknowledged/v1`, `delivery: ordinary-repository-policy`. No STATUS was called after the burn.

## Earlier pre-C1 environment-transport verification
- The pre-C1 environment-transport candidate was 194 diff lines (123 additions + 71 deletions) across seven files, within the provider's 200-line bound. Changed paths: the OpenSpec design, Inno script, `cli.py`, `setup_assist.py`, `test_frozen_entry_order.py`, `test_inno_script.py`, and `test_setup_assist_protocol.py`.
- Focused tests passed: **84 passed** across `test_setup_assist_protocol.py` and `test_inno_script.py`. Final full suite passed: **947 passed, 3 skipped**. ISCC **6.7.3** compile-only validation passed before the final test/doc-only cleanup; the Inno implementation did not change afterward. The setup was not executed; `build/iscc-verify/` is ignored.
- Security path: `cli.main` dispatches only on non-empty `_YASB_SETUP_ASSIST_REQUEST`; nonce-only input falls through to ordinary invalid-argv handling. Inno fails closed if `SetEnvironmentVariableW` fails, before `Exec`. Independent verification confirmed the active elevated path no longer reaches request/result filesystem helpers; legacy definitions remain but are unreachable from the shipped CLI/installer path.
- Test contract: `test_frozen_entry_order.py` now verifies request-only dispatch; `test_setup_assist_protocol.py` verifies nonce-only does not dispatch. The parent explicitly authorized this one test adjustment after the full suite exposed the stale nonce-dispatch expectation.
- OpenSpec records environment request + bounded exit code as active and file exchange as inactive. Older file-flow text elsewhere is explicitly marked superseded. Slice 1 aligns typed operation objects with Python and deliberately omits `config-apply` until Slice 2 supplies explicit provider choices.
- No installer execution, VM proof, or test rerun occurred. The fresh native review approved and closed the exact candidate; no code correction, commit, or delivery occurred. C1 remains a functional no-op follow-up, and VM behavior remains unverified.

## VM proof mapping — pre-C1 read-only
- The lifecycle harness is under the S11b worktree's ignored `build/s11b-lifecycle/`. Its scenario manifests exercise setup/uninstall lifecycle only; none sets `_YASB_SETUP_ASSIST_REQUEST` directly. The S11b media still packages the old nonce + `request.json`/`result.json` include, not the S11a candidate. Existing ISO SHA-256: `43b6f036d2a3ca2775d906a25b42153631bcf11d4d50b7a0beb8f78ca312bbca`.
- There is no built S11a installer artifact; previous ISCC validation was compile-only. The pre-C1 candidate's Inno payload included `choices` and string operations, but Python required exactly `{schema, operations}` with typed operation objects, so it exited fail-closed. Candidate tests do not establish a functioning round-trip.
- Existing checkpoint identity is `clean-windows-running-watcher-v7-69c7ff5`; expected watcher SHA-256 is `57d7ce68ece311e8c58b1c1d29c8899535182cb628c5e98b9737961ae018a95c`. Historical Z6 evidence records that the E: issue was recovered (2 GB sequential write and failing chain-delta read passed) and the full VM moved to `D:\\01_System_Images\\Virtual_Machines\\` in 19.4 minutes; subsequent v7 cycles passed on D:. Current live VM name/state and storage health still require a read-only preflight before any new cycle.
- No VM cycle has been run. The user selected C1-before-VM after the preflight showed that the existing S11b media is stale and the uncorrected payload is rejected fail-closed. After C1 and code verification, fresh S11a input media and a live read-only VM/storage preflight are required before any VM cycle.

## VM proof outcome (S11a)

- Media: `s11a-inputs-r2.iso` `bee1b386...` (13,641,728 B), installer `ddfa6761...` (13,491,710 B), source commit `b363abe`, volume label `S11AINPUTS`.
- Checkpoint: `clean-windows-running-watcher-s11a-v2` (`6ec8fcb7-...`), frozen with the S11a media attached.
- Evidence: `build/s11a-lifecycle/evidence/` (6 scenarios, 10 run records, host gate PASS) plus two rejected-run archives (`evidence-pre-fix/`, `evidence-invalid-ckpt-reuse/`).
- Two rejected runs are recorded on purpose: the first reused the S11b checkpoint and silently executed the S11b installer (`7ff33e5d...`), because a checkpoint revert re-attaches the media recorded in that snapshot; the second used the correct media but the pre-fix installer, which aborted in `InitializeWizard` (`Control 'TNewComboBox(...)' has no parent window`). Only the per-run `executable.sha256` in the evidence exposed the first, and only the real install exposed the second.
- Bound review: lineage `review-b65826db306acab0`, target `sha256:2cc3940...`, 7 paths / 411 lines, approved with 7 advisory findings and acknowledged.

## Finding
The installer originally derived a predictable nonce from timestamp and computer name and used path-based request/result files under `%LOCALAPPDATA%\Temp`. The current candidate replaces the active elevated exchange with a validated environment request and bounded child exit code; the nonce-only CLI path is disabled. Legacy file helpers remain dead code. Slice 1 now aligns Inno's typed operations with Python without implicit provider mutation; `config-apply` remains intentionally deferred until Slice 2's explicit provider-selection UI. The prior host-side S11a test-only commit `72d18ca` remains unpushed.
