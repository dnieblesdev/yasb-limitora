# UTF-8
# Windows version resource for the frozen executable. Rendered by
# scripts/build_frozen_bundle.py from the single __version__ source; never
# hard-code a product version here.
VSVersionInfo(
  ffi=FixedFileInfo(filevers=@@VERSION_TUPLE@@, prodvers=@@VERSION_TUPLE@@, mask=0x3F, flags=0x0, OS=0x40004, fileType=0x1, subtype=0x0, date=(0, 0)),
  kids=[
    StringFileInfo([StringTable('040904B0', [
      StringStruct('CompanyName', 'dnieblesdev'),
      StringStruct('FileDescription', 'yasb-limitora frozen CLI'),
      StringStruct('FileVersion', '@@VERSION@@'),
      StringStruct('InternalName', 'yasb-limitora'),
      StringStruct('LegalCopyright', 'MIT License'),
      StringStruct('OriginalFilename', 'yasb-limitora.exe'),
      StringStruct('ProductName', 'yasb-limitora'),
      StringStruct('ProductVersion', '@@VERSION@@'),
    ])]),
    VarFileInfo([VarStruct('Translation', [1033, 1200])]),
  ],
)
